import 'package:flutter/material.dart';

class RegisterGymOwnerPage extends StatelessWidget {
  const RegisterGymOwnerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Register Gym Owner')),
      body: const Center(child: Text('Gym Owner Registration Page')),
    );
  }
}
