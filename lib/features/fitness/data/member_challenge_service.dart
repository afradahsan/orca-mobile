import 'package:orca/features/fitness/domain/member_challenge_model.dart';

class MemberChallengeService {
  Future<MemberChallengeProgress> getProgress(String challengeId, String token) {
    return 1 as Future<MemberChallengeProgress>;
  }

  Future<MemberChallengeProgress> logWorkout(String challengeId, String token) {
    return 1 as Future<MemberChallengeProgress>;
  }
}
